# 安全边界

fwm 是当前操作系统账户的 SSH 转发管理器，管理 IPC 不提供跨账户共享或独立的应用登录机制。同一账户内的其他程序、管理员/root 以及已攻陷的用户环境不在隔离边界内。

## 本地管理接口

- Unix 使用内核提供的 peer credentials，在客户端发送请求前核验 daemon UID，在后台读取请求前核验客户端 UID。长路径备用 socket 的目录与端点也要归当前账户所有；拒绝目录符号链接、端点符号链接和可被其他账户写入的目录。客户端只检查这些路径，不自动创建或修复权限。
- Windows 管道只允许当前 SID 访问并拒绝远程客户端。客户端核验已连接句柄的所有者和受保护、仅允许当前 SID 的 DACL，不仅相信可预测的管道名称。新管道和配置目录显式设置当前用户所有权。为兼容旧版提升权限运行的实例，提升权限的当前进程可以接受 Administrators 所有者；不会因此接受任意其他账户或用户组。
- 不能确认对端身份时拒绝请求。状态探测、后台启动和离线配置选择也传播鉴权错误；持续 watch 可以重试，但只能报告不可用状态，不能把缓存视作已验证运行状态。

## SSH 与远端回收

SSH 目标及每一级跳板都先验证主机密钥，再进行用户认证。未知或变更的主机密钥需要明确的信任操作；撤销记录不能被普通匹配记录覆盖。`IdentitiesOnly` 限制可用身份，不向配置中保存私钥内容或口令。

远端主动回收仅针对当前 SSH 账户拥有、登记为同一管理器与规则、再次核验进程出生身份及监听归属的旧独占会话。无法确认时拒绝回收，不按端口或进程名称杀死其他会话。

## 转发入口

转发监听是数据入口，不使用管理 IPC 的鉴权。SOCKS5 只支持无认证 CONNECT；默认监听回环地址，同机其他账户仍可能访问该端口。显式绑定其他地址时，应自行限制可访问该端口的网络范围。反向监听的实际地址还受 SSH 服务端 `GatewayPorts` 控制。

## 依赖告警跟踪

当前锁定的 `rsa 0.10.0-rc.18` 命中 [RUSTSEC-2023-0071](https://rustsec.org/advisories/RUSTSEC-2023-0071.html)。2026-09-21 复查时，上游尚无已修复版本。fwm 使用 RSA 的 SSH 签名和公钥验证路径，未发现可供网络对端调用的 PKCS#1 解密/去填充接口；因此不能仅凭依赖命中认定 fwm 存在可利用的 Marvin 解密攻击，也不能将该依赖笼统标注为已修复。

签名路径的私钥运算未启用 blinding；本轮没有验证签名侧可利用的时序泄露。更新 russh/ssh-key/rsa 时应重新核查调用路径和上游进展：[剩余侧信道跟踪](https://github.com/RustCrypto/RSA/issues/626)、[解密与签名范围说明](https://github.com/RustCrypto/RSA/pull/702)。
