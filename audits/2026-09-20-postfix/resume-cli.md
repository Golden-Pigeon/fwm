# CLI 续审：普通功能正确性

日期：2026-09-20。本轮是 review，没有修改生产文件。读取已有 `cli-api.md` 及 JSON 证据后，以 CodeGraph 复核状态加载、选择器、日志过滤、事件归属和分派路径。没有重放旧 API 探针，没有构造网络、SSH、agent 或服务测试。

## 结论与去重

原报告的六个编号保持不变；其中 C1 建议降为 P3，其他五项保留 P2。这里的保留表示既有证据及对应实现仍支持该问题，并不表示本轮重新执行了原复现。新增确认问题 **0**。

| 编号 | 本轮分类 | 结论与准确定位 |
| --- | --- | --- |
| C1 | P3，输入契约一致性 | `dispatch.rs:26–47` 对探测命令直接分派，`dispatch.rs:60–64` 才检查 request_id。既有证据仅证明空/过长 ID 的 Doctor 与 Ping 结果不同，没有证明正常 CLI 请求受损、越权、状态破坏或服务中断；原 P2 偏高。不要将其描述为安全问题。 |
| C2 | P2，接受的配置与记录预算不兼容 | `model.rs:458–462`、`:487–491` 未限制 ID 长度；`events.rs:166–167` 将历史写入失败仅写 tracing；`:180–186` 的首条超页事件可导致成功空页和游标前移。沿用原 `cli-api-round1.json`；本轮未重跑大 ID 或容量探针。限制说明：CLI 正常新建 ID 为 UUID，原触发条件是外部输入的超长但获接受的 ID，不应表述为所有普通配置都会丢日志。 |
| C3 | P2，状态与配置版本错配 | `status_watch.rs:19–21` 只刷新一次配置；`queries.rs:26–42` 用该配置挑选另一个 revision 的运行时行。`query_selection.rs:42–47` 的服务器/组 watch 也依赖当前配置，属于同一根因，不另计新问题。原确定性模拟证据有效。 |
| C4 | P2，异步事件上下文错配 | `events.rs:116–117` 丢弃源时间戳；`:143` 重新计时；`:152–164` 用当前 ID 标签补全旧事件；`history.rs:55–59` 覆盖外层服务器与组。旧证据同时包含 `event.server_id=server-a` 与外层 `server_id=server-b`，足以支持日志归错服务器。与运行时/事件专项复核合并，不重复计数。 |
| C5 | P2，watch 初次 IPC 故障隐藏保存配置 | `status_watch.rs:88–102` 已读到可用保存配置，但 `:120` 排除 unavailable 状态、`:126` 清空行，因而合法选择器隐藏规则，缺失选择器不立即报错。沿用旧模拟证据；本轮离线正常状态测试表明普通 stopped 路径不会复现，问题限于首次加载进入错误 fallback。 |
| C6 | P2，全量状态聚合过大 | `dispatch.rs:117–121` 无条件序列化全部规则；`status_watch.rs:17–18` 先获取全部状态，之后才由 `queries.rs:26–42` 按单条过滤。沿用 `status-budget-round3.json`，本轮不重跑原 SSH 配置解析触发器。原单条 status 的 stdout 为空是错误输出到 stderr 的既定行为，不是额外 JSON 缺陷。 |

## 本轮新增证据

`cargo build -p fwm --offline --locked` 成功。随后运行 `python3 audits/2026-09-20-postfix/resume-cli-local.py`：**17 项通过，0 项失败**。脚本仅通过当前 `target/debug/fwm` 读取临时目录中的手写配置与历史文件；没有后台、模拟 IPC 服务或网络夹具。验证前后原文件内容相同，未生成 daemon socket。

记录位于 `resume-cli-local.json`，脚本位于 `resume-cli-local.py`。覆盖：

- 当前规则/服务器名称优先于历史同名对象；旧规则名跨改名按 ID 关联；已删除对象可按保留 ID 查询。
- `logs GROUP` 与 `logs --group GROUP` 一致，按事件时组标签筛选；旧组仍可查询。
- tail 在筛选之后应用；tail 0 不重放旧记录；缺失日志选择器得到空结果。
- 离线 status 保留保存规则与组；缺失 status 选择器失败；status/logs 选择器互斥；负 tail 被参数解析拒绝。

## 排除项与覆盖限制

没有将历史同名对象被当前对象遮蔽、旧组标签不回写、日志空结果、CLI JSON 错误写 stderr 列为缺陷，均与现有接口行为一致。日志按规则和服务器 follow 的稳定 ID 行为已有单元测试及原流式证据，本轮只读复核，没有重复启动这些流式场景。

这是本次恢复后的一个遗漏边界轮次，**最后一轮零新增**；不表示原确认问题已修复，也不表示穷尽了所有功能。未覆盖真实服务、操作系统差异、SSH/认证、远端清理、API 畸形输入、容量压力及运行时连接行为。上述 C1/C2/C6 中超出本轮普通只读 CLI 的部分明确依赖原有证据，未重演。
